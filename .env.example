# 업비트 API 키 (5단계 실전용). 이 파일을 .env 로 복사해서 채울 것.
# .env 는 .gitignore 에 등록되어 있음 — 절대 커밋 금지.
UPBIT_ACCESS_KEY=
UPBIT_SECRET_KEY=

# 안전장치
UPBIT_MAX_ORDER_KRW=10000     # 1회 주문 상한 (원)
UPBIT_ALLOW_LIVE=false        # true 로 바꾸기 전까지 실주문 차단
